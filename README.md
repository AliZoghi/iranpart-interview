# Iranpart Interview

Nuxt starter configured with TypeScript, Tailwind CSS, Axios, Pinia, and lodash.

## Setup

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

The default public API base URL is configured in `.env.example`.
